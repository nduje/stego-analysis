# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Results/artifacts (regenerated; we don't version binary outputs)
results/*.png
results/_test_stego.png
# large per-image sweep tables (regenerated); the summaries + figures ARE committed
results/imperceptibility.csv
results/chisquare_scores.csv
results/chisquare_positional.csv
results/_*.csv
# note: results/*_summary.csv and results/figures/*.png ARE committed

# Dataset (downloaded/generated via scripts; we don't push images into the repo)
data/bossbase/
data/covers/*.pgm
data/alaska/raw_tif/
data/alaska/covers/
# note: data/alaska/manifest.csv (small) IS committed

# OS/IDE
.DS_Store
.idea/
.vscode/
