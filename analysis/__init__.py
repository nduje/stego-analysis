"""Steganalysis / detection utilities.

`detection` is a detector-agnostic evaluation harness (AUC, P_E, ROC) reused by
every detector (chi-square on Day 6; RS/SPA/ML on Days 7-10). `chi_square` is the
first concrete detector.
"""
