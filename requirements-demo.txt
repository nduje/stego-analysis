# Demo-only dependencies (kept separate so the measurement pipeline stays runnable
# without Streamlit). Install with:  pip install -r requirements-demo.txt
streamlit>=1.30
# the demo also uses the measurement stack already required by the project:
#   numpy, scipy, scikit-image, matplotlib, Pillow, cryptography
